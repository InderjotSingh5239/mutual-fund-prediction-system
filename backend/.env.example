# --- App ---
APP_NAME=MF-Intelligence-Platform
APP_ENV=development
DEBUG=true
API_V1_PREFIX=/api/v1

# --- Security ---
SECRET_KEY=change-this-to-a-long-random-secret-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- Database ---
POSTGRES_USER=mf_user
POSTGRES_PASSWORD=mf_password
POSTGRES_DB=mf_platform
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg2://mf_user:mf_password@db:5432/mf_platform

# --- Redis / Celery ---
REDIS_HOST=redis
REDIS_PORT=6379
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# --- CORS ---
# Include your deployed frontend URL(s) here in production, e.g.
# ["http://localhost:5173","https://your-app.vercel.app"]
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# --- External Data Sources (Phase 1 uses AMFI only, no key needed) ---
AMFI_NAV_URL=https://www.amfiindia.com/spages/NAVAll.txt

# --- Phase 3: Market Data + News Intelligence ---
# Yahoo Finance needs no key (used automatically).
# FRED: free key at https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=
# NewsAPI: free key at https://newsapi.org/register
NEWSAPI_KEY=

# --- Phase 4: Portfolio Analytics & Calculators ---
DEFAULT_TRADING_DAYS_PER_YEAR=252
DEFAULT_MONTE_CARLO_SIMULATIONS=5000
DEFAULT_RISK_FREE_RATE=0.065

