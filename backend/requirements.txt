# =========================================================
# MF Intelligence Platform — Unified Production Requirements
# Merged and deduplicated from Phases 1-5. Pinned to the
# highest compatible version observed across all phases.
# =========================================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Database ---
SQLAlchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
asyncpg==0.29.0

# --- Security / Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# --- Background jobs (ETL / ML scheduling) ---
redis==5.0.8
celery==5.4.0

# --- Observability ---
loguru==0.7.2

# --- HTTP / networking ---
httpx==0.27.2
tenacity==9.0.0

# --- Data & numerics ---
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1
python-dotenv==1.0.1

# --- Phase 2: Machine Learning / MLOps ---
scikit-learn==1.5.2
xgboost==2.1.1
lightgbm==4.5.0
prophet==1.1.5
tensorflow==2.17.0
optuna==4.0.0
shap==0.46.0
mlflow==2.16.2

# --- Phase 3: Market Data + News Intelligence ---
yfinance==0.2.44
fredapi==0.5.2
vaderSentiment==3.3.2

# --- Testing (kept in main requirements so CI/Docker test stages
#     don't need a second install step; heavier dev-only tooling
#     lives in requirements-dev.txt) ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
