# Base URL of your FastAPI backend's versioned API root. When unset, the app
# falls back to mock data (see src/services/*.ts).
VITE_API_BASE_URL=https://mutual-fund-prediction-system.onrender.com/api/v1
